# QuizLearn — Telegram Mini App 🧠

**QuizLearn** — это интерактивное образовательное веб-приложение, разработанное специально для использования внутри Telegram (Mini App). Приложение позволяет создавать собственные квизы, проходить их на время, работать над ошибками и делиться результатами с друзьями.

> Проект разработан в рамках курсовой работы по дисциплине «Основы программирования».

## ✨ Основные возможности

**Создание квизов:** Конструктор вопросов с настройкой времени на ответ.
**Прохождение:** Интерактивный режим с таймером, прогресс-баром и анимацией карточек.
**Работа над ошибками:** Уникальный режим «Мои ошибки», позволяющий перепройти только те вопросы, где были допущены ошибки в прошлом запуске.
**Шаринг:** Генерация специальных ссылок (Deep Links) для отправки квизов друзьям внутри Telegram.
**Статистика:** Отслеживание прогресса, количества пройденных тестов и среднего времени ответа.
**История:** Сохранение последних созданных и пройденных квизов[cite: 76].

## 🛠 Технологический стек

Проект реализован на клиент-серверной архитектуре.

### Frontend
**React** — библиотека для построения пользовательского интерфейса.
**Vite** — инструмент сборки.
**Telegram Web Apps API** — интеграция с мессенджером (получение `user_id`, обработка `start_param`).
**CSS Modules / Variables** — стилизация интерфейса.

### Backend
**FastAPI (Python)** — высокопроизводительный фреймворк для REST API.
**SQLAlchemy** — ORM для работы с базой данных.
**Pydantic** — валидация данных.

### База данных
**SQLite** — реляционная база данных для хранения пользователей, квизов, вопросов и результатов.

## 📂 Структура базы данных

Приложение использует следующие сущности:
* `User` — пользователи Telegram.
* `Quiz` — метаданные квиза (название, таймер, владелец).
* `QuizQuestion` — вопросы и ответы.
* `QuizResult` — результаты прохождений.
* `SharedQuiz` — токены для генерации ссылок-приглашений.
* `LastUserQuiz` — для быстрого запуска последнего квиза.

## 🚀 Установка и запуск

### Предварительные требования
* Node.js (v18+)
* Python (v3.10+)
* Telegram аккаунт

### 1. Настройка Backend

```bash
# Перейдите в папку сервера (предполагается ./backend)
cd backend

# Создайте виртуальное окружение
python -m venv venv

# Активируйте окружение
# Windows:
venv\Scripts\activate
# Mac/Linux:
source venv/bin/activate

# Установите зависимости
pip install fastapi uvicorn sqlalchemy pydantic

# Запуск сервера (по умолчанию http://localhost:8000)
uvicorn main:app --reload
```
### 2. Настройка Frontend

```bash
# Перейдите в папку клиента
cd frontend

# Установите зависимости
npm install

# Запуск в режиме разработки
npm run dev
```

### 📱 Запуск в Telegram
* Создайте бота через @BotFather.
* Создайте новое Web App, привязав его к HTTPS ссылке вашего фронтенда (можно использовать ngrok для туннелирования localhost).
* Для тестирования шаринга бот должен поддерживать deep linking (формат t.me/botname?startapp=TOKEN).

### 🔌 Описание API
| Метод | Путь | Описание  |
| ------------- | ------------- | ------------- |
| POST  | /quiz/create  | Создание нового квиза с вопросами |
| POST  | /quiz/start  | Запуск квиза (по ID или последнего активного) |
| GET  | /quiz/all  | Получение всех квизов пользователя |
| POST  | /quiz/result/save  | Сохранение результата прохождения |
| DELETE  | /quiz/{id}  | Удаление квиза (только владельцем) |
| GET  | /stats/{user_id}  | Получение статистики пользователя |
| POST  | /share/generate  | Генерация ссылки для шаринга |
| GET  | /share/{token}  | Получение квиза по токену шаринга |


### 👨‍💻 Автор

Сергиенко К.Р. Санкт-Петербургский государственный университет аэрокосмического приборостроения (ГУАП)
